/**
 * GCD Algorithm
 */ 

#include <math.h>
#include <stdio.h>
#include <assert.h>
#include <stdint.h>

int gcd_by_prime_fact(int prime_factors_a[], int idx_a, int prime_factors_b[], int idx_b) {
	
/*	
	int gcd = 0;
	for (int i = 0; i < idx_a; i++) {
		if 
	}*/
	/*
	while (b != 0) {
		int tmp = b;
		b = a % b;
		a = tmp;
	}
	
	return a;*/
	return 0;
}
