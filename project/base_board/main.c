#include "arm_math.h"
#include "stm32f4xx.h"
#include "cmsis_os.h"

/*!
 @brief Program entry point
 */
int main (void) {
	
	
	// The below doesn't really need to be in a loop
	while(1){
		osDelay(osWaitForever);
	}
}

